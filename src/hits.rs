pub mod hits {
    pub fn get_greatest_hits(path: String) -> String{
        let conn = Connection::open_in_memory();
        
        let hits: Vec<String> = Vec::new();

        for row in client.query("SELECT 
    }

}
